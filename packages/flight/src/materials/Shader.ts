import { Shader as RawShader } from '@flighthq/engine';
import FlightObject from '../FlightObject';

export default class Shader extends FlightObject<RawShader> {
  constructor() {
    super();
  }
}
